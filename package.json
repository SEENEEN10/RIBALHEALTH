{
  "name": "ribal-website",
  "version": "1.0.0",
  "private": true,
  "description": "RIBAL HealthTech marketing site — static frontend + Netlify Functions backend.",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "resend": "^4.0.0"
  }
}
